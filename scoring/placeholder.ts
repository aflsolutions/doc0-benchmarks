// Placeholder module so `tsc --noEmit` has at least one input before Task 2
// adds real scoring modules (tsc errors with "no inputs found" on empty
// include globs). Safe to delete once scoring/*.ts contains real code.
export {};
